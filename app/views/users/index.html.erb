<h1>this is index page of users</h1>
<div class="users-list">
  <% @users.each do |u| %>
    <p>
      User ID: <%= u.id %>,
      Username: <%= link_to u.username, user_path(u) %>
    </p>
  <% end %>
</div>
